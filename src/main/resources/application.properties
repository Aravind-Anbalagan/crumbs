# ============================================================
#  CRUMBS TRADING APP — APPLICATION CONFIGURATION
#  ⚠️  PRODUCTION values are set by default
#  ⚠️  Scroll to bottom to change values for LOCAL run
# ============================================================

# Application
spring.application.name=crumbs_new
server.port=${PORT:8080}
server.address=0.0.0.0
app.max-threads=5
spring.main.allow-circular-references=true
spring.main.banner-mode=off
spring.web.resources.add-mappings=true
server.forward-headers-strategy=framework
app.data.path=/app/data
# Logging
logging.config=classpath:logback-spring.xml
logging.level.root=OFF
logging.level.com.crumbs.trade=DEBUG

# Database — Supabase PostgreSQL
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres?prepareThreshold=0&sslmode=require
spring.datasource.username=postgres.hmyvcnxlycjvzopxloie
spring.datasource.password=Athiran@2020
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.keepalive-time=60000
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.connection-init-sql=SET statement_timeout TO 120000

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=anbalagan.aravind@gmail.com
spring.mail.password=fqrd jvpn wzks qlii
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=15000
spring.mail.properties.mail.smtp.timeout=15000
spring.mail.properties.mail.smtp.writetimeout=15000

# AI — Groq (OpenAI compatible)
spring.ai.openai.api-key=${OPENAI_API_KEY}
spring.ai.openai.base-url=https://api.groq.com/openai
spring.ai.openai.model=moonshotai/kimi-k2-instruct
spring.ai.retry.max-attempts=3
spring.ai.retry.initial-interval=1000

# Swagger
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs

# Telegram
telegram.bot-token=${TELEGRAM_API_KEY}
telegram.chat-id=1685538818
telegram.base-url=https://api.telegram.org

# AngelOne
angelone.api.url=https://margincalculator.angelbroking.com/OpenAPI_File/files/OpenAPIScripMaster.json

# File Paths
app.local.resources.path=src/main/resources

# Actuator
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never
management.health.db.enabled=false
management.health.mail.enabled=false
management.health.diskspace.enabled=false

# ============================================================
#  ⚠️  LOCAL RUN — CHANGE THESE VALUES BEFORE RUNNING LOCALLY
#  ⚠️  REVERT BEFORE COMMITTING & PUSHING TO PRODUCTION
# ============================================================

# PROD  → scheduler.enabled=true
# LOCAL → scheduler.enabled=false
scheduler.enabled=true
scheduling.enabled=true

# PROD  → spring.devtools.restart.enabled=false
# LOCAL → spring.devtools.restart.enabled=true
spring.devtools.restart.enabled=false